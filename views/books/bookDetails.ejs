<!--<center><h2><%= book.title %></h2></center>

<div>
  <body>
    <center>
      <img
        id="currentImg"
        onmouseover="largerIm()"
        onmouseout="normalSize()"
        src="<%= book.coverImagePath %>"
        height="350"
        width="300"
      />
    </center>
    <center>
        <b>publisher:</b> <%= book.publisher %>
        <b>publish date:</b> <%= book.publishDate%>
        <b>price:</b> <%= book.price %>
    </center>
  </body>
  <script>
    function largerIm() {
      document.getElementById('currentImg').height = '800';
      document.getElementById('currentImg').width = '800';
    }
    function normalSize() {
      document.getElementById('currentImg').height = '350';
      document.getElementById('currentImg').width = '300';
    }
  </script>

  <form action="/add-to-cart/<%= book.id %>" method="POST">
    <center><button type="submit">Add to cart</button></center>
    <%= book.description %>
    <div><button type="submit">Leave Review</button></div>
  </form>
</div>
-->
<div class="row">
  <div class="col-sm-6 col-md-4">
    <div class="thumbnail">
      <img src=<%= book.coverImagePath %> alt="..." class="img-responsive">
      <div class="caption">
        <h3><%= book.title %></h3>
        <h2><%= book.author %></h2>
          <p class="description"><%= book.description %></p>
          <div class="clearfix">
            <div class="price pull-left"><%= book.price %></div>
              <a href="/add-to-cart/<%=book._id%>" class="btn btn-success pull-right" role="button">Button</a>
          </div>
      </div>
  </div>
</div>